import type { ImageSourcePropType } from 'react-native';

/**
 * Default course row thumbnail (Imbewu mark) — same asset and treatment as the public catalogue / first page.
 */
export const COURSE_LOGO_THUMB: ImageSourcePropType = require('../../assets/images/icon.png');
